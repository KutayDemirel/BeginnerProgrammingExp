        int num,i =2;

        printf("Please Enter a number   :");
        scanf("%d",&num);
        for(i;i<sayi;i++){
            if(sayi%i==0){
                break;
            }}
        printf("Number %d",i);
        if(sayi ==i)
            printf(" is Prime Number");
        else
            printf(" is not Prime Number");

    return 0;
}
